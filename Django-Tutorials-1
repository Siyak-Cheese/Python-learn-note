# Set a target that build a website use django and python.

********* Chapter one *********

  > pip install django
    # install package of dj
  > django-admin
    # list dj command
  > django-admin startproject django_project
    # build a project of empty
  > tree
    # show the django_project construct
    __init__.py # tell python this is a python project
    settings.py # project settings and configurations
    urls.py # we'll set up the mapping from certain URLs to where we sent the user
    wsgi.py # tell us how our Python web application and the web server communicate
    manage.py # the main python file of django project
  > python manage.py runerver
    # run project
  > http://127.0.0.1:8000
    # localhost of local machine
  > http://localhost:8000/admin